import { Request, Response } from 'express';

// Recupera as informações CreateUserService.ts
class CreateUserController {
  async handle(request: Request, response: Response) {
    
  }
}

export { CreateUserController }